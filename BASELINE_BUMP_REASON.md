# ESLint baseline ceiling bump log

This file is the public-facing audit log for every upward bump of the
ESLint suppression ceilings enforced by
[`scripts/check-eslint-baseline.ts`](scripts/check-eslint-baseline.ts).

The ceilings themselves live as literals at the top of that script, and
the live suppression counts are tracked in
[`eslint-suppressions.json`](eslint-suppressions.json) (regenerated by
ESLint's suppression command). The script blocks any commit that sets a
`RULE_CEILINGS[<rule>]` (or `TOTAL_CEILING`) **above** the most recent
`new ceiling` recorded in the table below for that rule.

## How to bump a ceiling

1. Decide whether the bump is genuinely necessary. The default is to
   remove the new suppression(s) by typing the offending code instead.
2. If the bump must happen, add a new row to the table below in the
   same commit. The row must include:
   - `rule` — the rule key as it appears in `RULE_CEILINGS` (use
     `TOTAL` for the aggregate ceiling).
   - `old ceiling` — the previous recorded value (last `new ceiling`
     for that rule in this table).
   - `new ceiling` — the value you are setting in the script.
   - `delta` — `new - old` (positive for a bump, signed).
   - `reason` — one short sentence explaining why the bump is
     justified (e.g. "test mocks for new admin email-change UI").
   - `commit/task ref` — the task number (e.g. `#684`) or commit SHA
     that introduced the suppressions.
3. Update `RULE_CEILINGS` / `TOTAL_CEILING` in
   `scripts/check-eslint-baseline.ts` to match the new value.
4. Run `tsx scripts/check-eslint-baseline.ts --strict` to confirm the
   gate accepts the change.

Decreases (ratchet-downs) and unchanged ceilings do **not** require a
new row — the table only grows on bumps. The script reads the most
recent row per rule as the "recorded ceiling"; as long as the script's
literal is `<=` the recorded value, the gate stays green.

## Bump history

| rule | old ceiling | new ceiling | delta | reason | commit/task ref |
| --- | --- | --- | --- | --- | --- |
| @typescript-eslint/no-explicit-any | 0 | 0 | 0 | Retroactive baseline; rule paid down to zero in tasks #329 / #384 and held there since. | #384 |
| @typescript-eslint/no-non-null-assertion | 0 | 220 | +220 | Retroactive baseline. Originally seeded at 232 by #371, raised through the CI green-up pass (test-only `value!` patterns in admin email-change UI tests, system-admin trust-proxy probe tests, and sanitize/payment/charges receipt tests) up to 258, then ratcheted back down to the current 220 in #683 (244→220 via `if (!x) throw` guards). | #371, #683 |
| @typescript-eslint/no-unnecessary-type-assertion | 0 | 88 | +88 | Retroactive baseline. Seeded at 89 by #371, raised to 92 in the CI green-up pass (mock-construction casts in test files), ratcheted 92→91→89→88→85 across #572/#646/#683, then restored 85→88 in #684 because #683 ratcheted the ceiling without regenerating `eslint-suppressions.json`. | #371, #684 |
| @typescript-eslint/consistent-type-assertions | 0 | 4 | +4 | Retroactive baseline. Seeded by #371 at 4 (object-literal `{...} as Foo` casts) and unchanged since. | #371 |
| no-restricted-syntax | 0 | 153 | +153 | Retroactive baseline. Seeded at 125 by #371 (the `as unknown as Foo` double-cast matcher), raised through the CI green-up pass to 159 (test mocks in trust-proxy / email-change / sanitize / charges / payment receipt tests), ratcheted 159→154→153→150 across #572/#610/#683, then restored 150→153 in #684 for the same reason as `no-unnecessary-type-assertion`. | #371, #684 |
| TOTAL | 0 | 487 | +487 | Retroactive aggregate baseline = 220 (no-non-null-assertion) + 88 (no-unnecessary-type-assertion) + 4 (consistent-type-assertions) + 153 (no-restricted-syntax) + 22 pre-existing `no-undef` in `client/public/sw.js`. Last set in #684. | #371, #684 |
| react-hooks/exhaustive-deps | 0 | 14 | +14 | Seeded when eslint-plugin-react-hooks was wired into the lint gate; pre-existing missing/unnecessary-dep violations baselined rather than risk-edited (dep changes can alter effect/memo behavior). | react-hooks/query lint wiring |
| @tanstack/query/exhaustive-deps | 0 | 7 | +7 | Seeded when @tanstack/eslint-plugin-query was wired into the lint gate; pre-existing missing-queryKey-dep sites baselined for deliberate per-site staleness review rather than blanket auto-fix. | react-hooks/query lint wiring |
| @tanstack/query/no-unstable-deps | 0 | 5 | +5 | Seeded when @tanstack/eslint-plugin-query was wired into the lint gate; pre-existing unstable-dep (useMutation/useQueries passed into a dep array) sites baselined. | react-hooks/query lint wiring |
| TOTAL | 487 | 509 | +22 | Wired eslint-plugin-react-hooks + @tanstack/eslint-plugin-query into the lint gate: +14 react-hooks/exhaustive-deps, +7 @tanstack/query/exhaustive-deps, +5 @tanstack/query/no-unstable-deps. Clean rules (rules-of-hooks + 0-violation query rules) added with no suppressions. | react-hooks/query lint wiring |
